name: V-MINE Dragonfly Runtime

on:
  push:
    branches:
      - x86_64-guest
  workflow_dispatch:

jobs:
  dragonfly-runtime:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Checkout Dragonfly
        uses: actions/checkout@v4
        with:
          repository: df-mc/dragonfly
          ref: master
          path: dragonfly

      - uses: actions/setup-go@v5
        with:
          go-version-file: dragonfly/go.mod
          cache-dependency-path: dragonfly/go.sum

      - name: Install host dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y clang lld meson ninja-build libsqlite3-dev libarchive-dev

      - name: Build V-MINE and Dragonfly
        shell: bash
        run: |
          set -euo pipefail
          meson setup build-arm64 -Dguest_arch=arm64 --buildtype=release
          ninja -C build-arm64
          wget -q -O alpine-arm64.tar.gz https://dl-cdn.alpinelinux.org/alpine/v3.24/releases/aarch64/alpine-minirootfs-3.24.0-aarch64.tar.gz
          mkdir dragonfly-root
          tar -xzf alpine-arm64.tar.gz -C dragonfly-root
          (
            cd dragonfly
            CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go build -trimpath -ldflags='-s -w' -o ../dragonfly-root/vmine-dragonfly .
          )
          cp dragonfly/config.toml dragonfly-root/config.toml
          chmod 755 dragonfly-root/vmine-dragonfly
          tar --numeric-owner -C dragonfly-root -cf dragonfly-rootfs.tar .
          ./build-arm64/tools/fakefsify dragonfly-rootfs.tar dragonfly-fakefs

      - name: Run and verify Dragonfly UDP 19132
        shell: bash
        run: |
          set +e
          setsid timeout --foreground 600 ./build-arm64/ish -f dragonfly-fakefs /bin/sh -c 'cd / && exec /vmine-dragonfly' >dragonfly.log 2>&1 &
          vmine_pid=$!
          set -e
          for attempt in $(seq 1 120); do
            if grep -Eqi ':[[:space:]]*4ABC[[:space:]]' /proc/net/udp /proc/net/udp6 2>/dev/null; then
              echo udp-19132 > dragonfly-startup.ok
              break
            fi
            if ! kill -0 "$vmine_pid" 2>/dev/null; then break; fi
            sleep 5
          done
          if [ -f dragonfly-startup.ok ]; then kill -- "-$vmine_pid" 2>/dev/null || true; fi
          set +e
          wait "$vmine_pid"
          runtime_rc=$?
          set -e
          tail -n 300 dragonfly.log
          echo "DRAGONFLY_RUNTIME_RC=$runtime_rc"
          test -f dragonfly-startup.ok
          echo 'V-MINE DRAGONFLY STARTUP: UDP 19132 OPEN'

      - name: Upload Dragonfly logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: vmine-dragonfly-runtime
          path: |
            dragonfly.log
            dragonfly-startup.ok
