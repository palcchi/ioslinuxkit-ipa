# Bug report

Describe the failure and the smallest command that reproduces it.

## Environment

- Source revision (`git rev-parse HEAD`):
- Host or iOS version:
- Release or debug build:
- Rootfs and package versions:
- Relevant environment variables:

## Reproduction

```text
Exact commands
```

## Result

- Exit status:
- Actual output:
- Expected output:

For `Bad system call`, `Illegal instruction` or `Segmentation fault`, include the relevant guest `dmesg` output. For an iOS crash, attach the symbolicated crash report from **Settings → Privacy & Security → Analytics & Improvements → Analytics Data** when available.

Do not put security vulnerabilities, credentials or private user data in a public issue. Use the private reporting route in [SECURITY.md](SECURITY.md).
